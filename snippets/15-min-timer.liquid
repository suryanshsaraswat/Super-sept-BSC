 <style>
   .countdofif {
    position: absolute;
    z-index: 1;
    top: 6px;
    left: 6px;
    background: #ff6803;
    padding: 0 3px;
    border-radius: 6px;
    font-weight: 600;
}
 </style>
    <div class="countdofif" id="countdofif">
        <!-- <span class="days">00</span> -->
        <!-- <span class="hours">00</span> -->
        <span class="minutes">00</span>
        <span class="seconds">00</span> <span>left</span>
    </div>

    <script>
       const fifteenMinutes = 15 * 60 * 1000; // 15 minutes in milliseconds

function updatecountdown() {
    const currentDate = new Date();
    
    // Define the target date as 1 PM (13:00) every day
    const targetDate = new Date(currentDate);
    targetDate.setHours(13, 0, 0, 0);

    // Calculate the time remaining until 1 PM
    let timeRemaining = targetDate - currentDate;

    if (timeRemaining <= 0) {
        clearInterval(countdownInterval);
        document.getElementById('countdofif').innerHTML = 'Offer has ended';
        return;
    }

    if (timeRemaining > fifteenMinutes) {
        // Display "Offer starts in X minutes" before 1 PM
        const minutes = Math.floor(timeRemaining / (1000 * 60));
        document.getElementById('countdofif').innerHTML = 'Offer starts in ' + minutes + ' minutes';
    } else {
        // Display the countdown timer
        const minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);
        document.querySelector('.minutes').innerText = formatTimeUnit(minutes) + 'm';
        document.querySelector('.seconds').innerText = formatTimeUnit(seconds) + 's';
    }
}

function formatTimeUnit(unit) {
    return unit < 10 ? `0${unit}` : unit;
}

// Update the countdown initially
updatecountdown();

// Update the countdown every 1 second
const countdownInterval = setInterval(updatecountdown, 1000);

    </script>
