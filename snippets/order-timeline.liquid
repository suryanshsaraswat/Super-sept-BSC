
<style>
 .progress-container {
            width: 80%;
            margin: 20px auto;
            position: relative;
            margin-bottom: 88px;
        }

        .timeline {
            width: 100%;
            height: 10px;
            background-color: #ccc;
            position: relative;
        }

        .progress {
            height: 100%;
            background-color: #4CAF50;
            position: absolute;
            transition: width 1s;
        }

        .trigger {
            position: absolute;
            height: 20px;
            width: 20px;
            border: 2px solid #4CAF50;
            background-color: #fff;
            border-radius: 50%;
            top: -5px;
            transform: translateX(-50%);
            cursor: pointer;
        }
        .trigger-label {
            text-align: center;
            font-size: 14px;
            position: absolute;
            top: 25px;
            transform: translateX(-50%);
        }
        
        
    </style>
    <div class="progress-container">
        <div class="timeline">
          <div class="progress" id="progress"></div>
          <div class="trigger" id="Open" style="left: 0;"></div>
          <div class="trigger-label" style="left: 0;">Order<br>confirmed</div>
          <div class="trigger" id="Picked" style="left: 25%;"></div>
          <div class="trigger-label" style="left: 25%;">Order<br>Packed</div>
          <div class="trigger" id="Shipped" style="left: 50%;"></div>
          <div class="trigger-label" style="left: 50%;">Order<br>Dispatched</div>
          <div class="trigger" id="orderOutOfDelivery" style="left: 75%;"></div>
          <div class="trigger-label" style="left: 75%;">Out for<br>Delivery</div>
          <div class="trigger" id="Delivered" style="left: 100%;"></div>
          <div class="trigger-label" style="left: 100%;">Delivered</div>
        </div>
    </div>
    <script>
        function getOrderStatus(orderId) {
            const statuses = ["Open", "Picked", "Shipped", "Out Of Delivery", "Delivered"];
            let currentStatus = shipStatus;
            alert(currentStatus); 
            let currentIndex = statuses.indexOf(currentStatus);
            if (currentIndex !== -1) {
                const progress = document.getElementById("progress");
                const trigger = document.getElementById(currentStatus);
                progress.style.width = `${(currentIndex) * 25}%`;
                trigger.style.backgroundColor = "#4CAF50";
            }
        }

        // Call the getOrderStatus function with your order ID
        getOrderStatus("yourOrderID");
    </script>